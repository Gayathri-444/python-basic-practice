#problem 7: Write a program to print the multiplication table of the given number (N) up to ten multiples in the format "N x i = M".

n = int(input()) #user input
for i in range(1,11): #iterating through for loop
    print(str(n)+" x "+str(i)+" = "+str(i*n)) #printing the table format, and multipying the exact multiple.
